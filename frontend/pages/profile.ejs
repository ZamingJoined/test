<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= name %></title>
</head>
<body>
    <h1>profile page of <b><%= name %></b></h1>
    <div>
        <a href="/">main</a>
        <a href="/addNews">создать новость</a>
    </div>
    <div id="news_list">
        <% for( let i = 0; i < news.length; i++ ) { %>
            <div class="id">
                <h3> <a href="<%=news[i].link%>"><%= news[i].title %></a> </h3>
            </div>
        <% } %>
    </div>
</body>
</html>