<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= data.title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
        }
        h1 {
            color: #333;
        }
        p {
            color: #666;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <h1><%= data.title %></h1>
    <p><%= data.disciptions %></p>
    <p>ID: <%= data.id %></p>
    <p>User: <a href="<%= data.user_link %>"><%= data.username %></a></p>
    <p>User ID: <%= data.user_id %></p>
    <p>Tags: <%= data.key_tags.join(', ') %></p>
</body>
</html>
